import 'package:flutter/material.dart';

class TargetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Targets (Time Frame)")),
      body: Center(
        child: Text("Target creation UI (extend later)"),
      ),
    );
  }
